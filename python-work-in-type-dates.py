x = input('Ввод ') #Функция принимает значения из полля ввода(Возвращает строчный формат)
#r = x + 5 #Будет ошибка
r = int(x) + 5 #Переведет в целое число(если может)
#int(x) переводит в целое число
#float(x) переводит в число с плавающей точкой
#str(x) переводит в строку
print(r)


float(5) #Перевед число 5 в 5.0
int(5.9) #Переведет число 5.9 в 5(дробная часть будет откинута)